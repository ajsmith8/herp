<%= form_for @topic do |f| %>
  <div class="field">
	<%= f.label "New topic (topic)" %><br />
    <%= f.text_area :content, rows: 2 %>
  </div>

  <div class="actions">	   
	<%= f.submit "Submit New Topic" %>	
  </div>

<% end %>