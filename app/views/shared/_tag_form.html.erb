<%= form_for @tag do |f| %>
  <div class="field">
	<%= f.label "Add tag (topic)" %><br />
    <%= f.text_field :content %>
  </div>

  <div class="field">  
	<%= f.hidden_field :topic_id, value: @topic.id %>
  </div>

  <div class="actions">	   
	<%= f.submit "Add Tag" %>	
  </div>

<% end %>