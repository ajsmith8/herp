<%= form_for(@tag) do |f| %>
  <div class="field">
    <%= f.label "New tag" %><br />
    <%= f.text_field :content, data: {autocomplete_source: tags_path} %>
  </div>
  <div class="actions">
	<%= f.hidden_field :topic_id, value: @topic.id %>
    <%= f.submit %>
  </div>
<% end %>
