 <div id="bookmark_form">
 <% if Bookmark.where(topic_id: @topic.id, user_id: current_user.id).count == 0 %>  
   <%= render 'users/bookmark' %>
 <% else %>
   <%= render 'users/unbookmark' %>
 <% end %>
 </div>